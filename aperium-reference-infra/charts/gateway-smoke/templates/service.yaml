apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-smoke.fullname" . }}
  labels:
    {{- include "gateway-smoke.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gateway-smoke.selectorLabels" . | nindent 4 }}
